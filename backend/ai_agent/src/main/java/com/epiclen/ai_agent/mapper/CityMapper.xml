<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.epiclen.ai_agent.mapper.CityMapper">
    <resultMap id="BaseResultMap" type="com.epiclen.ai_agent.entity.City">
        <id column="id" property="id" jdbcType="INTEGER" />
        <result column="name" property="name" jdbcType="VARCHAR" />
        <result column="state" property="state" jdbcType="VARCHAR" />
        <result column="country" property="country" jdbcType="VARCHAR" />
    </resultMap>

    <sql id="Base_Column_List">
        id, name, state, country
    </sql>

    <insert id="insert" parameterType="com.epiclen.ai_agent.entity.City">
        INSERT INTO city (
            id, name, state, country
        )
        VALUES (
            #{ id,jdbcType=INTEGER },
            #{ name,jdbcType=VARCHAR },
            #{ state,jdbcType=VARCHAR },
            #{ country,jdbcType=VARCHAR }
        )
    </insert>

    <delete id="deleteByPrimaryKey">
        DELETE FROM city
        WHERE id = #{ id,jdbcType=INTEGER }
    </delete>

    <update id="updateByPrimaryKey" parameterType="com.epiclen.ai_agent.entity.City">
        UPDATE city
        SET
            name = #{ name,jdbcType=VARCHAR },
            state = #{ state,jdbcType=VARCHAR },
            country = #{ country,jdbcType=VARCHAR }
        WHERE id = #{ id,jdbcType=INTEGER }
    </update>

    <select id="selectByPrimaryKey" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List" />
        FROM city
        WHERE id = #{ id,jdbcType=INTEGER }
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List" />
        FROM city
    </select>
</mapper>